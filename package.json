{
  "name": "telegram-quest-rewards",
  "version": "1.0.0",
  "description": "Telegram Mini App with gamified quest system",
  "scripts": {
    "start": "npm run start:backend",
    "start:all": "concurrently \"npm run start:backend\" \"npm run start:frontend\" \"npm run start:bot\"",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run dev",
    "start:bot": "cd bot && npm start",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../bot && npm install",
    "deploy": "npm run build:frontend && netlify deploy --dir=frontend/dist --prod",
    "deploy:preview": "npm run build:frontend && netlify deploy --dir=frontend/dist"
  },
  "keywords": [
    "telegram",
    "mini-app",
    "quest",
    "rewards",
    "gamification"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
